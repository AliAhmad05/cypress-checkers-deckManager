{
  "name": "checkers-deckmanager",
  "version": "1.0.0",
  "description": "A Cypress-powered test framework for validating the Checkers game UI and managing cards deck via API.",
  "scripts": {
    "test:report": "npx cypress run --reporter mochawesome",
    "test:report:open": "npx cypress run --reporter mochawesome && open mochawesome-report/mochawesome.html",
    "test:report:open:chrome": "npx cypress run --reporter mochawesome --browser chrome && open mochawesome-report/mochawesome.html"
  },
  "author": "Ali Ahmad",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.15.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
